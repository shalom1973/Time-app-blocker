name: app_blocker
description: אפליקציה לחסימת יישומים
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # חבילות שהוספנו
  device_apps: ^2.2.0
  permission_handler: ^11.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true