workflows:
  android-workflow:
    name: Find Project Structure
    max_build_duration: 5
    scripts:
      - name: List all files recursively
        script: |
          echo "==== Listing all files and directories to find the project root ===="
          ls -R
          echo "==== End of file list ===="